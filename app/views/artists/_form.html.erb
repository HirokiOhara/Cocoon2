<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :record_label %>
    <%= form.text_field :record_label %>
  </div>

  <h3>CDs</h3>
  <div id="cds">
    <%= form.fields_for :cds do |cd| %>
      <%= render 'cd_fields', f: cd %>
    <% end %>
    <div id="links">

      <%= link_to_add_association 'add cd',form, :cds %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
